export const BaseUrl = 'https://swapi.dev/api';

export enum FieldTypes {
    TEXT = 'text',
    NUMBER = 'number',
    DATE = 'date',
};

export const categories = ['people', 'planets', 'films', 'starships', 'vehicles'];
